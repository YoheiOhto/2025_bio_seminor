# 2025_bio_seminor
## how to update 
in local repo, run the following commands:
```shell
git add .
git commit -m "your message"
git push origin 251205_dev
```
in github, make a pull request from branch `251205_dev` to branch `main`  
after approval, merge the pull request